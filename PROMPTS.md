# AI-assisted development — prompt sequence

Fill this in with your actual prompt history before submitting (the assignment says
this may be requested). Suggested structure:

1. Initial scaffold prompt (project setup, component breakdown)
2. Listing page layout + styling prompts
3. Photo Tour overlay prompt
4. Lightbox + keyboard navigation + focus trap prompt
5. Accessibility pass prompt
6. Pixel-matching adjustment prompts (colors, spacing, fonts, animation timing)
7. Any bug-fix / refinement prompts

Keep the actual chat log or export it, since this doc should reflect what you
really ran, not a template.
